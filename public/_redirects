# Cloudflare Pages SPA fallback: requests to paths that don't match a static
# file fall through to index.html, then Expo Router's client-side routing
# resolves the URL (including dynamic routes like /decks/<id>).
# Existing static files (e.g. /home.html, /sw.js, /icon.png) are served as-is.
/*    /index.html   200
